import React from 'react'
import { Backdrop } from '@material-ui/core'
export default function PickNhanKhau() {
    return (
        <Backdrop
  sx={{ color: '#000', zIndex: (theme) => theme.zIndex.drawer + 1 }}
  open={true}
  onClick
>
 
</Backdrop>
    )
}
